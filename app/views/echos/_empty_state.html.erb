<%# Empty state partial - supports both Turbo Frame and CSS-only approaches %>

<div class="empty-state empty-state--only-child">
  <p class="empty-state__text">
    You don't have any echoes yet!
  </p>

  <%= link_to "Add echo",
              new_echo_path,
              class: "btn btn--primary",
              data: { turbo_frame: dom_id(Echo.new) } %>
</div>
